<h1><%= @doctor.name %></h1>
<h2>Appointments</h2>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= appt.display_datetime %> - <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
  <% end %>
</ul>
